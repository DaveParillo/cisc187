#include <iostream>

int main() {
  std::cout << "Hello, World!\n";
  return 0;  // considered a bad practice
             // to omit the return value from main
             // but it is legal to do so
}

